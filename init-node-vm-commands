    1  sudo apt-get install openssh-server
    2  exit
    3  ls
    4  sudo apt-get install openssh-server
    5  sudo service ssh status
    6  sudo vi /etc/ssh/sshd_config
    7  sudo ufw allow 7777
    8  sudo service ssh restart
    9  exit
   10  sudo apt-get update
   11  sudo apt-get install nginx
   12  sudo ufw app list
   13  sudo ufw allow 'Nginx HTTP'
   14  sudo ufw status
   15  systemctl status nginx
   16  sudo vi /etc/default/ufw
   17  sudo ufw default deny incoming
   18  sudo ufw default allow outgoing
   19  sudo ufw allow ssh
   20  sudo ufw allow 3000
   21  sudo ufw allow 7777
   22  sudo ufw enable
   23  sudo ufw allow 80
   24  sudo ufw allow 3000:3100/tcp
   25  sudo ufw status numbered
   26  sudo ufw status verbose
   27  exit
   28  sudo apt-get update
   29  sudo apt-get install nodejs
   30  sudo apt-get install npm
   31  nodejs -v
   32  sudo npm install -g n
   33  sudo n latest
   34  nodejs -v
   35  n
   36  n -v
   37  n --version
   38  sudo n latest
   39  nodejs -v
   40  curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -
   41  sudo apt-get install -y nodejs
   42  nodejs -v
   43  ssh-keygen
   44  ls
   45  more ~/.ssh/id_rsa.pub 
   46  exit
   47  sudo vi /etc/nginx/sites-enabled/concreet
   48  git clone https://github.com/bennekrouf/deploy.git
   49  ls
   50  mkdir src
   51  git clone git@bitbucket.org:concreet/concreet-api.git
   52  git clone git@bitbucket.org:concreet/concreet-web.git
   53  npm i -g pm2
   54  cd
   55  git clone git@bitbucket.org:concreet/concreet-containers.git
   56  mkdir ~/.npm-global
   57  npm config set prefix '~/.npm-global'
   58  echo "export PATH=~/.npm-global/bin:$PATH" >> ~/.profile
   59  source ~/.profile
   60  npm i -g pm2
   61  history
   62  git status
   63  git
   64  ls
   65  cd deploy/
   66  ls
   67  history > init-node-vm-commands
